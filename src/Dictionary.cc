#include "Dictionary.h"

Dictionary::Dictionary(int n) {}
int Dictionary::pesquisaDic(Dictionary *dic, Verbete *it) { return 0; }
int Dictionary::insereDic(Dictionary *dic, Verbete *it) { return 0; }
int Dictionary::imprimeDic(Dictionary *dic) { return 0; }
int Dictionary::atualizaDic(Dictionary *dic, Verbete *it) { return 0; }
int Dictionary::removeDic(Dictionary *dic, Verbete *it) { return 0; }
int Dictionary::destroiDic(Dictionary *dic) { return 0; }