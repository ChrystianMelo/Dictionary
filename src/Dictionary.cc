#include "Dictionary.h"

Dictionary::Dictionary(int n) {}
Dictionary::~Dictionary() {}
int Dictionary::pesquisaDic(Verbete *it) { return 0; }
int Dictionary::insereDic(Verbete *it) { return 0; }
int Dictionary::imprimeDic() { return 0; }
int Dictionary::atualizaDic(Verbete *it) { return 0; }
int Dictionary::removeDic(Verbete *it) { return 0; }